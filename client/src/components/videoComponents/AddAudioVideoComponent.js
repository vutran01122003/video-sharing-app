import { View, Text } from "react-native";

export default function AddAudioVideoComponent({ navigation, route }) {
    return (
        <View style={{ height: "100%" }}>
            <Text>Add Audio Video Component</Text>
        </View>
    );
}
