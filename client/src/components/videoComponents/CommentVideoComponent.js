import { View, Text } from "react-native";

export default function CommentVideoComponent({ navigation, route }) {
    return (
        <View style={{ height: "100%" }}>
            <Text>Comment Video Component</Text>
        </View>
    );
}
