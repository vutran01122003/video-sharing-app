import { View, Text } from "react-native";

export default function SelectFilterVideoComponent({ navigation, route }) {
    return (
        <View style={{ height: "100%" }}>
            <Text>Select Filter Video Component</Text>
        </View>
    );
}
