import { View, Text } from "react-native";

export default function AvatarComponent({ navigation, route }) {
    return (
        <View style={{ height: "100%" }}>
            <Text>Avatar Component</Text>
        </View>
    );
}
