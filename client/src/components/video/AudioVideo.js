import { SafeAreaView, Text } from "react-native";

export default function AudioVideo() {
    return (
        <SafeAreaView>
            <Text>Add Audio Video Component</Text>
        </SafeAreaView>
    );
}
