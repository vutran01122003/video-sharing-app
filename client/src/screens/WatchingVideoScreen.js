import { View, Text } from "react-native";

export default function WatchingVideoScreen({ navigation, route }) {
    return (
        <View style={{ height: "100%" }}>
            <Text>Watching Screen</Text>
        </View>
    );
}
