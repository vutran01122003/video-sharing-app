import { SafeAreaView, Text } from "react-native";

export default function StreamingVideoScreen({ navigation, route }) {
    return (
        <SafeAreaView style={{ height: "100%" }}>
            <Text>Streaming Screen</Text>
        </SafeAreaView>
    );
}
